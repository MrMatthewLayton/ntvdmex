#
# wow32ret.txt -- ANSWER AN UNIMPLEMENTED WOW32 CALL DIFFERENTLY. GH #128.
#
# One `<hex id> <hex dword>` per line. Every overridden call is logged as
#   `** wow32ret.txt OVERRIDE -- an EXPERIMENT, not a service **`
# so no reader can mistake a measurement for an implementation. 53 of the 82
# WOW32 ids are not named by krnl386's export table, and the sentinel we answer
# them with is load-bearing: 0 is right for "declined / not present" and WRONG
# for a caller that loops until the answer is non-zero.
#
# ** THIS FILE IS DELIBERATELY EMPTY, AND THAT IS THE MILESTONE. **
# It used to carry one line, `7d 00000001`, without which krnl386 spun 1884
# times in the retry loop at seg2:0x2a08 -- allocate, ask 0x7d whether the
# result is acceptable, on 0 allocate another -- leaking two bytes of stack per
# iteration until a #SS. With the line, WOWEXEC.EXE loaded and ran, and the
# furthest point this project has reached depended on a text file.
#
# 0x7d IS NOW A SERVICE (src/wow/wow32.h). Its two call sites are both inside
# krnl386's task-database creator, the retry offers ALIASES of the same memory
# so the question can only be about the selector's numeric value, and the retry
# path uses the return AS the selector -- so the answer is to echo the argument,
# not to say 1. A run with this file empty must reach exactly as far as a run
# with the old line in it; if it does not, the reading above is wrong.
#
# DATA LINES FIRST, COMMENTS BELOW -- the same rule pmbp.txt learned the hard
# way, and that this file then broke by putting its one data line at the end of
# 1350 bytes of explanation. Nothing that matters goes below a comment.
